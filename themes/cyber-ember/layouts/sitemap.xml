{{- printf "<?xml version=\"1.0\" encoding=\"utf-8\" standalone=\"yes\"?>" | safeHTML }}
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
  {{- range .Data.Pages }}
  {{- if not .Params.private }}
  <url>
    <loc>{{ .Permalink }}</loc>
    {{- if not .Lastmod.IsZero }}
    <lastmod>{{ .Lastmod.Format "2006-01-02T15:04:05-07:00" }}</lastmod>
    {{- end }}
    <changefreq>{{ cond .IsHome "weekly" (cond (eq .Section "blog") "weekly" "monthly") }}</changefreq>
    <priority>{{ cond .IsHome "1.0" (cond (or (eq .Section "writeups") (eq .Section "blog")) "0.8" "0.6") }}</priority>
  </url>
  {{- end }}
  {{- end }}
</urlset>
